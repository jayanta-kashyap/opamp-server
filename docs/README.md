# UI Screenshot

To add a screenshot of the dashboard:

1. Open the UI: http://localhost:4321
2. Take a screenshot of the dashboard
3. Save it as `dashboard-preview.png` in this folder

The README.md references this image at `docs/dashboard-preview.png`
